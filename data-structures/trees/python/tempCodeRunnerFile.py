 # def get_tags_and_levels(self, element, level=0):
    #     tags = [(element.tag, level, element.text)]
    #     for child in element:
    #         tags.extend(self.get_tags_and_levels(child, level + 1))
    #     return tags